import { PdfUploadInterceptor } from './pdf-upload.interceptor';

describe('PdfUploadInterceptor', () => {
  it('should be defined', () => {
    expect(new PdfUploadInterceptor()).toBeDefined();
  });
});
